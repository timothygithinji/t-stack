# dependencies
node_modules

# build output
dist
*.tsbuildinfo

# synced template files (canonical source: packages/templates/files)
apps/cli/templates

# pack backup (prepack writes a clean package.json; postpack restores)
apps/cli/package.json.publish-bak
apps/cli/*.tgz

# tooling caches
.vitest-cache
coverage
.turbo

# os + editor
.DS_Store
.idea
*.swp
*.swo

# logs + tmp
*.log
.npm
.bun

# secrets / env (never commit)
.env
.env.local
.env.*.local
.dev.vars
.t-stack-meta

# pulumi
Pulumi.*.yaml.bak
pulumi-state-*.json

# smoke artifacts
smoke-*/
