{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**",
        ".output/**",
        ".vinxi/**",
        "files/**",
        "src/lib/generated/**",
        "templates/**"
      ],
      "inputs": [
        "src/**",
        "scripts/**",
        "presets/**",
        "files/**",
        "package.json",
        "tsconfig.json",
        "vite.config.ts",
        "wrangler.jsonc",
        "components.json"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "inputs": [
        "src/**",
        "test/**",
        "presets/**",
        "files/**",
        "vitest.config.ts"
      ]
    },
    "lint": {
      "outputs": []
    }
  }
}
